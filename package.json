{
  "name": "backstage-soundcheck",
  "version": "0.1.0",
  "private": true,
  "description": "ZeroPath Backstage plugins monorepo",
  "repository": {
    "type": "git",
    "url": "https://github.com/ZeroPathAI/backstage-soundcheck.git"
  },
  "homepage": "https://github.com/ZeroPathAI/backstage-soundcheck#readme",
  "bugs": {
    "url": "https://github.com/ZeroPathAI/backstage-soundcheck/issues"
  },
  "license": "Apache-2.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "backstage-cli repo build --all",
    "build:all": "backstage-cli repo build --all",
    "lint": "backstage-cli repo lint --since origin/main",
    "lint:all": "backstage-cli repo lint",
    "test": "backstage-cli repo test",
    "clean": "backstage-cli repo clean"
  },
  "devDependencies": {
    "@backstage/cli": "^0.27.0"
  },
  "engines": {
    "node": ">=18"
  }
}
