app:
  title: ZeroPath Security Demo
  baseUrl: http://localhost:3000

  # Extension configuration - enable/disable and configure extensions
  extensions:
    # ZeroPath standalone page at /zeropath
    - page:zeropath: true

    # ZeroPath navigation item in sidebar
    - nav-item:zeropath: true

    # ZeroPath entity cards (shown on entity pages)
    - entity-card:zeropath/info:
        config:
          filter:
            kind: Component
    - entity-card:zeropath/summary:
        config:
          filter:
            kind: Component

    # ZeroPath entity content tab (Security tab on entity pages)
    - entity-content:zeropath/security:
        config:
          path: /security
          title: Security
          filter:
            kind: Component

organization:
  name: Demo

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: better-sqlite3
    connection: ':memory:'

integrations:
  github:
    - host: github.com
      token: ${GITHUB_TOKEN}

proxy:
  # ZeroPath API proxy is configured in app-config.local.yaml

auth:
  providers:
    guest: {}

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]
  locations:
    # ZeroPath demo entities (paths relative to packages/backend)
    - type: file
      target: ../../examples/entities.yaml

    # Organizational data
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group]

permission:
  enabled: true

# Soundcheck configuration
soundcheck:
  # Load check definitions from file
  checks:
    $include: ./soundcheck/checks/zeropath-checks.yaml
  # Load track definitions from file
  tracks:
    $include: ./soundcheck/programs/zeropath-program.yaml
